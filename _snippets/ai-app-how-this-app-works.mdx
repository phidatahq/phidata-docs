## How this App works

The streamlit apps are defined in the `app` folder and the `Assistants` powering these apps are defined in the `ai/assistants` folder.

Checkout the files in the `ai/assistants` folder for more information.
