---
title: Yfinance
---

**YFinanceTools** enable an Agent to access stock data, financial information and more from Yahoo Finance.

## Example

The following agent will provide information about the stock price and analyst recommendations for NVDA (Nvidia Corporation).

```python cookbook/tools/yfinance_tools.py
from phi.agent import Agent
from phi.tools.yfinance import YFinanceTools
from phi.llm.openai import OpenAIChat

agent = Agent(
    name="Finance Agent",
    llm=OpenAIChat(model="gpt-4-turbo"),
    tools=[YFinanceTools(stock_price=True, analyst_recommendations=True, stock_fundamentals=True)],
    show_tool_calls=True,
    description="You are an investment analyst that researches stock prices, analyst recommendations, and stock fundamentals.",
    instructions=["Format your response using markdown and use tables to display data where possible."],
)
agent.print_response("Share the NVDA stock price and analyst recommendations", markdown=True)

```

## Toolkit Params

<ResponseField name="stock_price" type="bool" default="True">
  Enables the functionality to retrieve current stock price information.
</ResponseField>
<ResponseField name="company_info" type="bool" default="False">
  Enables the functionality to retrieve detailed company information.
</ResponseField>
<ResponseField name="stock_fundamentals" type="bool" default="False">
  Enables the functionality to retrieve fundamental data about a stock.
</ResponseField>
<ResponseField name="income_statements" type="bool" default="False">
  Enables the functionality to retrieve income statements of a company.
</ResponseField>
<ResponseField name="key_financial_ratios" type="bool" default="False">
  Enables the functionality to retrieve key financial ratios for a company.
</ResponseField>
<ResponseField name="analyst_recommendations" type="bool" default="False">
  Enables the functionality to retrieve analyst recommendations for a stock.
</ResponseField>
<ResponseField name="company_news" type="bool" default="False">
  Enables the functionality to retrieve the latest news related to a company.
</ResponseField>
<ResponseField name="technical_indicators" type="bool" default="False">
  Enables the functionality to retrieve technical indicators for stock analysis.
</ResponseField>
<ResponseField name="historical_prices" type="bool" default="False">
  Enables the functionality to retrieve historical price data for a stock.
</ResponseField>

## Toolkit Functions

<ResponseField name="get_current_stock_price">
  This function retrieves the current stock price of a company.
</ResponseField>
<ResponseField name="get_company_info">
  This function retrieves detailed information about a company.
</ResponseField>
<ResponseField name="get_historical_stock_prices">
  This function retrieves historical stock prices for a company.
</ResponseField>
<ResponseField name="get_stock_fundamentals">
  This function retrieves fundamental data about a stock.
</ResponseField>
<ResponseField name="get_income_statements">
  This function retrieves income statements of a company.
</ResponseField>
<ResponseField name="get_key_financial_ratios">
  This function retrieves key financial ratios for a company.
</ResponseField>
<ResponseField name="get_analyst_recommendations">
  This function retrieves analyst recommendations for a stock.
</ResponseField>
<ResponseField name="get_company_news">
  This function retrieves the latest news related to a company.
</ResponseField>
<ResponseField name="get_technical_indicators">
  This function retrieves technical indicators for stock analysis.
</ResponseField>
