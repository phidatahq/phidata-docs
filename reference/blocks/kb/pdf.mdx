---
title: PDF KnowledgeBase
sidebarTitle: PDF
---

## Example

<Note>

Install `pypdf` if needed using [this guide](/how-to/python-libraries)

</Note>

```python knowledge_base.py
from phi.knowledge.pdf import PDFKnowledgeBase, PDFReader
from phi.vectordb.pgvector import PgVector

from resources import vector_db

pdf_knowledge_base = PDFKnowledgeBase(
    path="data/pdfs",
    # Table name: llm.pdf_documents
    vector_db=PgVector(
        collection="pdf_documents",
        db_url=vector_db.get_db_connection_local(),
    ),
    reader=PDFReader(chunk=True),
)
```

## PDFKnowledgeBase Reference

<ResponseField name="path" type="Union[str, Path]">
  Path to `PDF` files.
Can point to a single PDF file or a directory of PDF files.
</ResponseField>
<ResponseField name="reader" type="PDFReader">
  A `PDFReader` that converts the `PDFs` into `Documents` for the vector database.
</ResponseField>

## AssistantKnowledge Reference

`PDFKnowledgeBase` is a subclass of the `AssistantKnowledge` class and has access to the same params

<Snippet file="kb-base-reference.mdx" />
