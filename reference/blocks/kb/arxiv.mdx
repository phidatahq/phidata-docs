---
title: Arxiv KnowledgeBase
sidebarTitle: Arxiv
---

## Example

<Note>

Install `arxiv` using [this guide](/how-to/install)

</Note>

```python knowledge_base.py
from phi.knowledge.arxiv import ArxivKnowledgeBase
from phi.vectordb.pgvector import PgVector

from resources import vector_db

knowledge_base = ArxivKnowledgeBase(
    queries=["Generative AI", "Machine Learning"],
    # Table name: llm.arxiv_documents
    vector_db=PgVector(
        collection="arxiv_documents",
        db_url=vector_db.get_db_connection_local(),
    ),
)
```

## ArxivKnowledgeBase Params

<ResponseField name="queries" type="List[str]">
  Queries to search
</ResponseField>
<ResponseField name="reader" type="ArxivReader">
  A `ArxivReader` that reads the articles and converts them into `Documents` for
  the vector database.
</ResponseField>

## AssistantKnowledge Params

`ArxivKnowledgeBase` is a subclass of the `AssistantKnowledge` class and has access to the same params

<Snippet file="kb-base-reference.mdx" />
