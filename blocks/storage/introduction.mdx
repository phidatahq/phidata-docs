---
title: Introduction
---

Every `Conversation` comes with built-in memory but it only lasts while the session is active. To provide memory across sessions, we can store the `Conversation` to a `PostgreSQL` table.
